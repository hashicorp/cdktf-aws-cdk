# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: automerge
on:
  pull_request_target:
    types:
      - labeled
      - unlabeled
      - synchronize
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked
  pull_request_review:
    types:
      - submitted
  check_suite:
    types:
      - completed
  status: {}
jobs:
  automerge:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: automerge
        uses: pascalgn/automerge-action@v0.14.2 # TSCCR: no entry for repository "pascalgn/automerge-action"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          MERGE_RETRIES: "20"
          MERGE_RETRY_SLEEP: "60000"
